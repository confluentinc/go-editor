name: go-editor
lang: unknown
lang_version: unknown
git:
  enable: true
github:
  enable: true
  repo_name: confluentinc/go-editor
semaphore:
  enable: true
  pipeline_enable: false
  triggers:
  - tags
  - branches
  branches: []
